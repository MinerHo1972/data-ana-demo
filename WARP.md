# WARP.md

This file provides guidance to WARP (warp.dev) when working with code in this repository.

## Repository Status

This is a new/empty Git repository. The following sections will need to be updated as the codebase develops.

## Common Commands

### Git Operations
```bash
# Initialize repository with first commit
git add .
git commit -m "Initial commit"

# Add remote origin (replace with actual repository URL)
git remote add origin <repository-url>
git push -u origin main
```

### Development Setup
```bash
# Commands will be added here once project structure is established
```

### Building and Testing
```bash
# Build commands will be added here once build system is configured
```

### Linting and Formatting
```bash
# Linting commands will be added here once linters are configured
```

## Architecture Overview

This section will be populated once the codebase structure is established.

### Project Structure
To be documented when files and directories are created.

### Key Components
To be documented when components are implemented.

### Data Flow
To be documented when application logic is implemented.

## Development Guidelines

### Getting Started
1. This repository is currently empty
2. Initialize your project structure based on your chosen technology stack
3. Update this WARP.md file with project-specific information
4. Set up CI/CD pipelines as needed

### Important Files to Update This WARP.md With
- README.md (when created)
- package.json, requirements.txt, Cargo.toml, or similar dependency files
- Build configuration files (Makefile, build.gradle, etc.)
- CI/CD configuration files (.github/workflows/, .gitlab-ci.yml, etc.)
- Project-specific documentation

## Notes for Future Updates

When populating this repository:
1. Update the "Common Commands" section with actual build, test, and development commands
2. Document the high-level architecture in the "Architecture Overview" section
3. Include any project-specific development workflows
4. Add any special environment setup requirements
5. Document integration patterns and key abstractions
6. Remove this "Notes for Future Updates" section once the project is established